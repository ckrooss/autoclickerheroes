language: python
python:
  - "2.7"
  - "3.5"
install:
  - pip install -r requirements.txt
addons:
  apt:
    sources:
    - ubuntu
    packages:
    - libopencv-dev
    - python-opencv
    - python-xlib

script: nosetests

